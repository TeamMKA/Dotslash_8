/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.generated.photos', 'images.generated.photos', 'raw.githubusercontent.com','res.cloudinary.com'],
    },
};

export default nextConfig;
